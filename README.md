# MDT 多端会诊系统 (MDT System)

MDT (Multi-Disciplinary Team) 是一个集成了实时音视频、AI 辅助诊疗、多端协同的医疗会诊系统。项目采用 Monorepo 架构，包含现代化的 React 前端与高性能 NestJS 后端。

## 🌟 核心特性

- **多端协同会诊**：支持 PC 端专家/助理实时协作，状态同步。
- **AI 辅助诊疗**：集成阿里云 DashScope (FunASR)，支持专家语音实时转录为文字。
- **全流程管理**：覆盖“会诊前准备 -> 专家问诊 -> 会诊后文书 -> 报告生成”全生命周期。
- **实时通信**：基于 WebSocket (Socket.io) 实现操作同步与消息广播。

## 📂 目录结构

```bash
MDT/
├── mdt-system/           # 项目源码根目录 (Monorepo)
│   ├── apps/
│   │   ├── web/          # 前端应用 (React + Vite + AntD)
│   │   └── api/          # 后端应用 (NestJS + Socket.io + Prisma)
│   ├── packages/
│   │   └── database/     # 共享数据库定义 (Prisma Schema)
│   └── docker-compose.yml # 基础服务编排 (Postgres, Redis)
└── TECHNICAL_DOCS.md     # 📚 详细技术说明文档
```

## 🚀 快速开始

### 1. 环境准备
- Node.js (v18+)
- Docker (用于启动数据库)
- 阿里云 DashScope API Key (用于语音识别)

### 2. 启动服务

```bash
# 1. 进入项目目录
cd mdt-system

# 2. 启动数据库与Redis
docker-compose up -d

# 3. 安装依赖
npm install

# 4. 配置环境变量
# 复制 .env 模板并填入 DASHSCOPE_API_KEY
cp apps/api/.env.example apps/api/.env 
# (如果没有 .env.example，请手动创建 apps/api/.env 并添加: DASHSCOPE_API_KEY=sk-xxx)

# 5. 初始化数据库
npx prisma generate
npx prisma db push

# 6. 启动开发服务器 (同时启动前后端)
npm run dev
```

- **前端地址**: http://localhost:5173
- **后端 API**: http://localhost:3000

## 📖 技术文档

关于系统架构、模块设计、API 交互流程及开发指南，请参阅 [TECHNICAL_DOCS.md](./TECHNICAL_DOCS.md)。
